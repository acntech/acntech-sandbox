### APP ###
app:
  cache:
    default-time-to-live: PT60S

### Spring ###
spring:
  # Application
  application:
    name: spring-boot-ehcache
  # Messages
  messages:
    basename: locale/messages

### Logging ###
logging:
  level:
    org.springframework: INFO
    no.acntech: DEBUG

### Server ###
server:
  port: 8080
  error:
    include-stacktrace: ALWAYS
    whitelabel:
      enabled: false
